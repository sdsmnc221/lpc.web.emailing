{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest",
    "test:unit": "vitest run tests/**/*.test.ts",
    "test:integration": "vitest run tests/server/services/airtable/*.integration.test.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.10",
    "@types/airtable": "^0.10.0",
    "airtable": "^0.12.2",
    "nuxt": "^3.17.5",
    "tailwindcss": "^4.1.10",
    "vue": "^3.5.16",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "sass-embedded": "^1.89.2",
    "vitest": "^1.0.0"
  }
}
